import React, {} from 'react'

const AuthIndex = function() {
    return (
        <section>
            <p>go ahead and login, or register if you're new</p>
        </section>
    );
}

export default AuthIndex